# 03_デザイントークン — 暁（akatsuki）

出典: `design-reference/MokusoApp.dc.html` の `TONES.akatsuki`（Claude Design プロトタイプ原本・SHA-256検証済み）。
本実装は**暁のみ**を採用する（オーナー決定・2026-07-06）。切替UIは実装しない。
朱（shu）・墨（sumi）の値は比較検討の記録として原本内に残っている。

---

## 1. カラー（:root のCSS変数として定義）

```css
:root {
  --bg:            #f7efe2;                  /* ページ背景（和紙・暁の生成り） */
  --paper:         #fdf8ee;                  /* カード地 */
  --ink:           #38302a;                  /* 本文の墨色 */
  --sub:           #9a8b78;                  /* 補助文字 */
  --line:          rgba(90,66,42,.15);       /* 罫線・枠線 */
  --accent:        #c8703c;                  /* 朱（印・数字・強調） */
  --accent-hi:     #dd8449;                  /* 印ボタンのハイライト */
  --accent-ink:    #a5552a;                  /* 濃い朱（落款・リンク） */
  --accent-soft:   rgba(200,112,60,.4);      /* 印影の内側リング */
  --accent-shadow: rgba(180,110,60,.3);      /* 印ボタンの影 */
  --night:         #2a2637;                  /* 会釈カードの夜空 */
  --night-ink:     #d9d0c4;                  /* 夜空の上の文字 */
  --night-sub:     #948b85;                  /* 夜空の上の補助文字 */
  --glow:          rgba(240,170,110,.6);     /* 朝焼けの光・会釈の灯り */
  --glow-ink:      #eab88a;                  /* 夜空の上のアクセント・会釈済みの点 */
}
```

トーン非依存の固定色:

| 用途 | 値 |
|---|---|
| 印ボタンの文字 | `#fdf3e4`（ボタン内リングは `rgba(255,244,230,.5)`、注記は `rgba(253,243,228,.78)`） |
| 光の点（通常） | `#f2e3c4`（微光: `rgba(242,227,196,.4)`） |
| 受信バナーの点 | `#f4d9a8` |
| 明転グラデーション | `linear-gradient(180deg, rgba(255,196,148,.9), rgba(255,231,203,.6) 45%, rgba(255,250,240,.15))` |
| 会釈カード内の罫線 | `rgba(255,255,255,.12)` |

## 2. タイポグラフィ

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;500;600;700&family=Zen+Old+Mincho:wght@400;700&display=swap" rel="stylesheet">
```

- 本文スタック: `'Noto Serif JP', 'Hiragino Mincho ProN', 'Yu Mincho', serif`（フォント不達でもページは完全動作）
- 見出し・印影用: `'Zen Old Mincho', serif`
- `-webkit-font-smoothing: antialiased`

| 要素 | サイズ / ウェイト / 字間 |
|---|---|
| ロゴ「黙走会」 | Zen Old Mincho 32px / 700 / .3em |
| 「minamo.run」 | 10.5px / .42em |
| ヘッダー一文 | 12px / .16em |
| リード「今日も、あなたの一歩を。」 | 12.5px / .26em |
| 印ボタン「走った印を押す」 | 縦書き 23px / 600 / .22em / line-height 2 |
| 印ボタン「一日一回まで」 | 9.5px / .34em |
| 印影「黙走」 | Zen Old Mincho 縦書き 46px / 700 / .28em |
| 落款（年・月日） | 縦書き 10.5px / .14em |
| 「これで n 日目です」 | 15px（数字のみ 33px / 500） |
| カード見出し | 14px / .14em（番号 .08em） |
| カード属性ラベル (非公開)等 | 10px / .14em |
| 大きな数字（累計日数・人数） | 38px / 500 / line-height 1 |
| 添え書き入力 | 17px（placeholder含む） |
| 脚注・案内文 | 10.5px / .08em / line-height 1.9 |

字間を空ける中央揃えテキストは `letter-spacing` と同値の `padding-left` で光学的センターを取る（原本の技法）。

## 3. 寸法・レイアウト定数

| 要素 | 値 |
|---|---|
| 設計幅 | 402px（コンテナ max-width 460px・中央寄せ） |
| ページ下余白 | 56px |
| ヘッダー上余白 | 76px |
| 印ボタン / 印影 | **180px 円形**。ボタン `rotate(-2deg)`・印影 `rotate(-3.5deg)` |
| ボタン内リング | inset 9px / 1.5px 線。印影内リング inset 8px / 1px 線・枠線 3px |
| 落款の位置 | 円の左右 26px・上下中央（縦書き） |
| 朝日のグロー | 190px 円・radial-gradient(--glow → transparent 68%)・opacity .5 |
| 丘のシルエット | --ink の opacity .06 / .05・height 64px / 46px・blur(7px)・border-radius 100% 100% 0 0 |
| カード | padding 20px 22px・radius 8px・枠線 1px --line・左右マージン 20px |
| 添え書きエディタ | padding 18px 20px・radius 8px・左右マージン 24px・要素間 gap 12px |
| 入力・ボタンの高さ | padding 13px（実効タップ高さ 約46px ≧ 44px） |
| 光の点フィールド | **height 188px**・点は 3〜6.5px・タップ領域は不可視の **44×44px**（margin -22px で中心合わせ） |
| 点の配置範囲 | left 5〜94% / top 10〜86%（中央寄りの正規分布風: `50 + (r()-r())*46 / *42`） |
| 表示点数 | `min(38, max(6, count))` |

## 4. アニメーション（この4つだけ）

```css
@keyframes mkFlash   { 0%{opacity:0} 14%{opacity:.95} 45%{opacity:.7} 100%{opacity:0} }   /* 明転 1.25s ease-out forwards */
@keyframes mkPulse   { 0%,100%{transform:scale(1);opacity:.85} 50%{transform:scale(1.3);opacity:1} }  /* 会釈の灯り 2.6s〜3s */
@keyframes mkAppear  { from{opacity:0;transform:translateY(6px)} to{opacity:1;transform:translateY(0)} } /* 印影・エディタの出現 .6〜.7s */
@keyframes mkTwinkle { 0%,100%{filter:brightness(.85)} 50%{filter:brightness(1.25)} }      /* 点の明滅 2.4〜5.4s（点ごとに乱数） */
```

- 明転はJS側で1350ms後に要素を除去（`mkFlash 1.25s ease-out forwards`）
- ボタン押下: `:active { transform: rotate(-2deg) scale(.96); }`（transition transform .15s ease）
- **常時動く装飾はこの点の明滅のみ**。それ以外のループアニメーションを追加しない
- `@media (prefers-reduced-motion: reduce)` で mkFlash / mkTwinkle / mkPulse を無効化する

## 5. 点（光）の疑似乱数 — 原本から移植する関数

同じ日は誰が見ても同じ星空になるよう、日付文字列をシードにする決定的乱数を使う
（FNV-1a ハッシュ → mulberry32。`design-reference/MokusoApp.dc.html` の `rng(seedStr)` をそのまま移植。
シードは `todayKey()` の日付文字列）。

## 6. 落款の漢数字変換 — 原本から移植する関数

- 年: 各桁を `〇一二三四五六七八九` で置換（2026 → 二〇二六）
- 月日: 十進漢数字（`kanjiNum()`: 7→七、10→十、12→十二、21→二十一）→ 「七月十二日」
- 原本の `kanjiNum(n)` と sealY / sealMD 生成ロジックをそのまま移植

## 7. 将来メモ（実装はしない）

- Google Fonts はCookieを設定しないが第三者リクエストではある。静けさを徹底したくなったら
  サブセットフォントの自己ホスト（`public/fonts/`）に切替可能。Phase 1 では複雑化を避けて採用しない
