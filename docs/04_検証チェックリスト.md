# 04_検証チェックリスト

各 Step の受け入れ基準は [00_計画書.md](00_計画書.md) に記載。本書はリリース前に通す横断検証。
**§4（非送信性）と §5（非追跡監査）はリリース前必須。**

---

## 1. 層1 と JST日付

- [ ] 押印 → DevTools Application タブで `minamo.total / last / note / sumKm` を目視確認
- [ ] **日付ロールオーバー試験**: コンソールで `localStorage.setItem('minamo.last','2026-07-11')` にして再読込
      → ボタンが再び押せる / total は保持される / 「途切れ」的な表示・文言が一切出ない
- [ ] **JST境界の単体確認**（コンソールで実行）:
      ```js
      new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo'}).format(new Date('2026-07-12T14:59:00Z')) // → "2026-07-12"
      new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo'}).format(new Date('2026-07-12T15:00:00Z')) // → "2026-07-13"
      ```
      （UTC 15:00 = JST 0:00 で日付が変わる）。クライアントと Worker が**同一式**であることをコードレビューで確認
- [ ] 添え書きの書き直し: 5km で保存 → 3km に書き直し → sumKm が +3 になっている（+8 になっていない）
- [ ] 落款の漢数字: 2026-07-12 に「二〇二六」「七月十二日」と出る（10日・20日・21日等の桁も机上確認）

## 2. 層2（今日の人数）

- [ ] `curl -X POST <API>/api/stamp` を2回 → `curl <API>/api/today` で count=2・dots に2件
- [ ] 5分キャッシュの遅延を仕様として確認（即時反映しないことは正常。「数字は目安」）
- [ ] `wrangler kv key list --binding MINAMO_KV` で**全キーに expiration が付与**されている
- [ ] 翌日（またはTTL経過後）に同コマンドでキーが消滅している → **リセット処理ゼロの実証**

## 3. 層3（会釈の送受信）

- [ ] 窓A（通常）と窓B（シークレット）で各々押印 → Aの点フィールドに他者の点が現れる
      → Aが一点をタップ → Bを再読込 → 「今日のあなたの走りに、誰かが会釈しました。」が灯る
- [ ] **受信側に数がどこにも出ない**（バナーは文言のみ・点の数と受信は無関係）
- [ ] Aが同日中に2点目をタップできない（キャプションが「そっと、会釈を残しました。〜」に変わっている）
- [ ] curl で同一 `to` に POST /api/bow を連打 → Bの表示は変わらない（1ビットの実証）
- [ ] 存在しない `to` への bow が 404 で、`kv key list` にゴミキーが増えない
- [ ] GET /api/bow に一昨日以前の date を渡すと 400
- [ ] 自分の点が自分の画面でタップ対象になっていない（dots から自ID除外）

## 4. ファイル解析の非送信性（最重要検証）

1. [ ] DevTools Network タブを開いてクリア → GPX / TCX / FIT を取り込み →
       **新規リクエスト0件**（初回のみ `js/fit-reader.js` の同一オリジンGETは許容）。
       スクリーンショットを撮り、本ファイル末尾の「検証記録」に貼る
2. [ ] DevTools オフラインモードで同じ操作が成功する（物理的にネットワークへ出ようがない証明）
3. [ ] 解析コードのレビュー: ハンドラ到達範囲に fetch / XMLHttpRequest / sendBeacon / WebSocket が存在しない
4. [ ] 抽出しているのが距離・時間の2値のみ（ルート座標・心拍を変数に取り出していない）
5. [ ] `#d=5.2&t=31` 付きで開く → 欄が埋まる → アドレスバーからフラグメントが即座に消えている

## 5. 非追跡・プライバシー監査

### ページ側
- [ ] 初回ロードの Network タブ: 同一オリジン + fonts.googleapis.com / fonts.gstatic.com **のみ**
- [ ] Application タブ: **Cookie 0件**。localStorage 以外のストレージ（IndexedDB等)未使用
- [ ] 分析タグ・エラートラッキング（GA / Sentry 等）のコードが存在しない

### Worker側（grep監査）
- [ ] `scheduled` ハンドラが存在しない（cronゼロ）
- [ ] KV `delete` 呼び出しが存在しない（消すのはTTLだけ）
- [ ] `CF-Connecting-IP` がレート制限Mapの一時判定以外（put / console.log / レスポンス）に現れない
- [ ] `console.log` が本番コードに残っていない
- [ ] km / min / distance / duration 等、記録データのフィールド名が存在しない
- [ ] 会釈を数えるコード（カウンタ・配列蓄積・履歴）が存在しない
- [ ] wrangler.toml に `[observability]` / `[triggers]` が存在しない。ダッシュボードでも Logs / Analytics Engine が無効のまま

## 6. デグラデーション

3通り（`API_BASE=""` / Worker停止（偽URL）/ 機内モード）すべてで:
- [ ] 層1が完全動作（押印 → n日目 → 添え書き → 累計）
- [ ] #sec02 の人数が「—」表示・説明文はそのまま
- [ ] #sec03 がカードごと静かに非表示
- [ ] エラーダイアログ・トースト・スピナーが出ない。コンソールにエラーの山が出ない
- [ ] `file://` で index.html を直接開いても層1が成立する

## 7. UI・アクセシビリティ（静けさの品質）

- [ ] スマホ実機（またはDevToolsモバイル表示 402px）で全セクション崩れなし
- [ ] タップ領域: 印ボタン・添える/あとで・点（44×44px）・書き直すリンク — すべて片手・親指で押せる
- [ ] `prefers-reduced-motion: reduce` で明転・明滅が止まる
- [ ] 押印済み状態で再読込しても印影が維持される（「今日は、もう印を残しています」）
- [ ] ページ内grep: 「連続」「ストリーク」「streak」「ランキング」「位」が0件

## 8. 公開前の最終確認（Step 4 / Step 6 前夜）

- [ ] 公開URLでフル動作（層1〜3の一巡）
- [ ] Worker の ALLOWED_ORIGINS が本番URLのみ+ローカル（不要なオリジンが残っていない）
- [ ] リポジトリに秘密情報ゼロ（APIトークン・.dev.vars がコミットされていない）
- [ ] README の複製手順どおりに別フォルダへコピーして層1が動く
- [ ] KVが空の状態で「今日、共に走った人 0 人」ではなく初回の見え方が破綻しない（count=0 でも点フィールドは最少6点の淡い光）

---

## 検証記録

（実装セッションが実施日・結果・スクリーンショットをここに追記する）

### 2026-07-08 — Step 1+2 ローカル検証（実装セッション）

- 環境: `wrangler dev --port 8787`（ローカルKV）+ `python -m http.server 8898`（public/）
- §1 層1: 押印 → 印影（落款 二〇二六 / 七月八日）→ 添え書き 5.2km/31分 → 書き直し時の sumKm 再計算 ✅
- §2 層2: stamp×6 → Worker再起動（キャッシュクリア）後の today で count=6・dots 6件。
  5分メモリキャッシュによる表示遅延を仕様として確認 ✅
- §3 層3: ブラウザ押印 → 点タップ → bow送信 → 受信側で「今日のあなたの走りに、誰かが会釈しました。」
  （1ビット・数の表示なし）。同一宛の連打は状態不変・存在しないID 404・不正ID 400・一昨日の date 400・
  同日2点目のタップ不可 ✅
- §5 監査: Worker に scheduled / KV delete / console.log なし。CF-Connecting-IP はレート制限Mapの一時判定のみ。
  レート制限は10回/分超過で429 ✅。ページの外部リクエストは Google Fonts のみ。
  **添え書き保存時の新規ネットワークリクエスト0件（距離・時間は非送信）** ✅
- §6 デグラデーション: `API_BASE=""` で層1完全動作・#sec02「—」・#sec03 非表示・コンソールエラー0件 ✅
- 備考: ブランド名 minamo は綴りに「min」を含むため、§5 の km/min フィールド名grepは
  単語境界（`\bmin\b` 等）で行うこと
- 未実施（デプロイ後に §8 で確認）: 実TTLでの物理消滅・本番オリジンでのCORS・2窓（通常+シークレット）での実会釈

### 2026-07-08 — 本番デプロイ検証（実装セッション・§8の一部）

- デプロイ: Worker → https://minamo-api.junyamasaki.workers.dev / Pages → https://minamo-eyu.pages.dev
  （KV: MINAMO_KV `0482f756…`。作成直後は error 1042 が出るが1〜2分の伝播待ちで解消）
- 本番APIの一巡: stamp → today(count/dots) → bow → 受信確認 bowed:true ✅
- 本番ページ配信 200・API_BASE正・CORS: `Access-Control-Allow-Origin: https://minamo-eyu.pages.dev` 返却 ✅
- `Cache-Control: public, max-age=300` 付与 ✅
- 本番KVのテストデータ（押印2・会釈1）はTTLで2026-07-10までに自動消滅する（7/12の初日はゼロから）
- 未実施: 実TTLでの物理消滅の目視（07-10に `wrangler kv key list --binding MINAMO_KV --remote` で確認可）・
  2窓での実会釈・実機スマホ確認

### 2026-07-09 — Step 3 検証（実装セッション・§4）

- 環境: `python -m http.server 8898`（public/）+ ヘッドレスChrome（file:// 検証）。
  層2の汚染防止のため、押印テスト前に `window.fetch` をスタブして本番APIへのPOSTを遮断した上で実施
- §4-1 非送信性: Network クリア → GPX×2 / TCX×2 / FIT×2 / D&D(FIT) を取り込み →
  **新規リクエストは初回 .fit 時の `js/fit-reader.js` 同一オリジンGET 1件のみ**。
  ファイルデータを運ぶリクエストは0件（記録はネットワークログの目視。POST /api/stamp も不発を確認）✅
- §4-2 オフライン: GPX/TCX は取り込み中の通信が構造的にゼロ（上記ログで実証）。FIT はリーダー読込済なら同様。
  さらに **file:// 直開き（サーバ非稼働 = 物理的に配信元なし）で fit-reader.js の注入と解析が成功**
  （ヘッドレスChromeで `OK km=5.2 min=31`）✅。
  補足: 初回の .fit 選択をオフラインで行うと fit-reader.js が読めず FILE_ERR に落ちる（遅延読み込みの構造上の限界・仕様）
- §4-3 コードレビュー: `fetch|XMLHttpRequest|sendBeacon|WebSocket` の grep — fit-reader.js **0件**、
  index.html は既存 apiFetch（層2/3）の1箇所のみで取り込みハンドラから到達不能 ✅
- §4-4 2値限定: fit-reader.js は session(18) の field 8/9 以外を一律 skip（lat/lon 0x85 のデコードなし・
  record(20) のデコードなし・developer fields はサイズ加算のみ）。GPX の座標保持は直前1点のスカラー2つのみ ✅
- §4-5 フラグメント: `#d=5.2&t=31` → URLから即消去 ✅。状態別:
  未押印=保留→押印直後にプリフィル / 押印済=エディタが開いてプリフィル / サマリー中=既存値を敷いた上に上書き
  （#d のみの片値でも既存の分が残る）✅。`#d=abc`・`#d=-5&t=-3` は静かに無視+URL消去、`#charter` は素通し ✅
- 解析値の検証（合成サンプル）: GPX 3点 0.009°刻み → 2km/12分（metadata の time 誤読なし）・
  trkseg 2分割 → セグメント間ギャップ約11kmを非加算 ✅ /
  TCX Lap×2 → 5.2km/31分（Trackpoint 累積値との二重加算なし）・Lap距離欠損 → Trackpoint 末尾累積へフォールバック ✅ /
  FIT 最小構成 → 5.2km/31分・複雑構成（record混在・圧縮タイムスタンプヘッダ・developer fields・
  無関係フィールド・session×2合算）→ 5.2km/31分 ✅ / 壊れたファイル(非XML・非FIT) → FILE_ERR 1種類のみ ✅
- 非自動保存: 全取り込み直後に `minamo.note` 不変 → 「添える」で初めて保存 ✅。
  書き直し保存で sumKm 再計算（5.2→9.9 で +9.9-5.2）の回帰も ✅
- コンソール: 全操作を通じて warning / error 0件 ✅
- 未実施: 実機スマホでのファイル選択導線・iOSショートカット実機（Step 5 で実施）
